-- Прибираємо колонку likes з таблиці posts.
-- Причина: в V7 ми створили таблицю post_likes яка зберігає хто саме лайкнув.
-- Тримати одночасно і лічильник (likes INTEGER) і окрему таблицю (post_likes) —
-- означає зберігати одні й ті самі дані двічі і постійно їх синхронізувати.
-- Кількість лайків тепер завжди рахується через: SELECT COUNT(*) FROM post_likes WHERE post_id = ?
ALTER TABLE posts DROP COLUMN likes;